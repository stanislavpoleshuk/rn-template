import React from "react";
import {StyleSheet, Animated} from "react-native";

interface AnimationHeaderNavigationProps {
    y: number;
}

type ComponentProps = AnimationHeaderNavigationProps;

export const AnimationHeaderNavigationComponent = (props: ComponentProps): React.ReactElement => {

    return (
        <Animated.View>

        </Animated.View>
    )
}

const styles = StyleSheet.create({
})