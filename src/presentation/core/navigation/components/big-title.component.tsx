import React from "react";
import {StyleSheet, Animated} from "react-native";

interface BigTitleProps {
    y: number;
}

type ComponentProps = BigTitleProps;

export const BigTitleComponent = (props: ComponentProps): React.ReactElement => {

    return (
        <Animated.View>

        </Animated.View>
    )
}

const styles = StyleSheet.create({
})